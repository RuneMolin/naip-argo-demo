# PRODUCTION is pinned to whatever tag has already been validated in staging —
# always older than or equal to staging's tag, never `main`.
cluster:
  name: production
environment:
  name: production
source:
  repoURL: https://github.com/RuneMolin/naip-argo-demo.git
  targetRevision: v1.0.0
  path: solution
